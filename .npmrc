# During the coordinated Mantine v9 wave, @polis/react@0.4.2 (an optional dev
# peer pulled from main) still upstream-pins @mantine/core ^7||^8, which
# ERESOLVE-conflicts with this theme's Mantine v9. Allow the install until
# polis-react's v9 release lands; the npm `overrides` handle eslint-plugin peers.
legacy-peer-deps=true
